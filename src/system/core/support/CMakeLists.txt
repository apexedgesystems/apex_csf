# Support components (inherit from SupportComponentBase)
# These are optional runtime services that any executive can opt into.

# Components (alphabetical)
add_subdirectory(data_transform)
add_subdirectory(shm_ring_bridge)
add_subdirectory(system_monitor)
add_subdirectory(telemetry_manager)
