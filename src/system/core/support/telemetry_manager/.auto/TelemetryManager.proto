// Generated by cdef_gen from the TelemetryManager spec -- DO NOT EDIT.
// Regenerate: make cdef (check-cdef diffs this file against the spec).
// Layout metadata rides apex/options.proto (standard FieldOptions
// extensions); declaration order is layout order.
syntax = "proto3";

package system_core.support;

import "apex/options.proto";

message TelemetryManagerHealthTlm {
  uint32 collectCount = 1 [(apex.default) = "0"];
  // Total telemetry packets pushed to bus.
  uint32 packetsSent = 2 [(apex.default) = "0"];
  // Failed pushes (queue full or registry miss).
  uint32 sendFailures = 3 [(apex.default) = "0"];
  // Number of active subscriptions.
  uint32 activeCount = 4 [(apex.width) = 2, (apex.default) = "0"];
  // Alignment padding.
  uint32 reserved = 5 [(apex.width) = 2, (apex.default) = "0"];
}

message TelemetryManagerState {
  uint32 collectCount = 1 [(apex.default) = "0"];
  // Total telemetry packets pushed to bus.
  uint32 packetsSent = 2 [(apex.default) = "0"];
  // Failed pushes (queue full or registry miss).
  uint32 sendFailures = 3 [(apex.default) = "0"];
  // Number of active subscriptions.
  uint32 activeCount = 4 [(apex.width) = 2, (apex.default) = "0"];
  // Alignment padding.
  uint32 reserved = 5 [(apex.width) = 2, (apex.default) = "0"];
}

message TelemetryManagerTprm {
  // Base collect task rate (Hz); subscriptions divide from this.
  uint32 collectRateHz = 1 [(apex.width) = 2, (apex.default) = "1"];
  // Explicit padding.
  uint32 reserved0 = 2 [(apex.width) = 2, (apex.default) = "0"];
  // Explicit padding.
  uint32 reserved1 = 3 [(apex.default) = "0"];
  // Push channel table; unused entries have active = 0.
  repeated TelemetrySubscription subscriptions = 4 [(apex.count) = 32];
}

message TelemetrySubscription {
  // Target component fullUid (e.g. 0x00D000).
  uint32 fullUid = 1 [(apex.default) = "0"];
  // DataCategory enum (0=STATIC, 1=TUNABLE, 2=STATE, 3=INPUT, 4=OUTPUT).
  uint32 category = 2 [(apex.width) = 1, (apex.default) = "0"];
  // 1 = active, 0 = inactive.
  uint32 active = 3 [(apex.width) = 1, (apex.default) = "0"];
  // APROTO opcode for the outbound telemetry packet.
  uint32 opcode = 4 [(apex.width) = 2, (apex.default) = "0"];
  // Byte offset into the data block (0 = start).
  uint32 offset = 5 [(apex.width) = 2, (apex.default) = "0"];
  // Bytes to read (0 = entire block).
  uint32 length = 6 [(apex.width) = 2, (apex.default) = "0"];
  // Rate divisor: push every N collect ticks (1 = every tick).
  uint32 rateDiv = 7 [(apex.width) = 2, (apex.default) = "1"];
  // Explicit padding.
  uint32 reserved = 8 [(apex.width) = 2, (apex.default) = "0"];
}
