// Generated by cdef_gen from the HilPlantModel spec -- DO NOT EDIT.
// Regenerate: make cdef (check-cdef diffs this file against the spec).
// Layout metadata rides apex/options.proto (standard FieldOptions
// extensions); declaration order is layout order.
syntax = "proto3";

package appsim.plant;

import "apex/options.proto";

message HilPlantState {
  // Elapsed simulation time [s].
  double simTime = 1;
  // Last computed altitude [m].
  double lastAlt = 2;
  // Last vertical velocity [m/s].
  double lastVz = 3;
  // Plant steps executed.
  uint32 stepCount = 4;
  // Control updates executed.
  uint32 ctrlCount = 5;
  // Telemetry reports sent.
  uint32 tlmCount = 6;
  // Comm loss events detected.
  uint32 commLossCount = 7;
  // Consecutive control frames with no new command.
  uint32 staleCmdFrames = 8;
  // Snapshot of driver rxCount for staleness detection.
  uint32 lastSeenRxCount = 9;
  // 1 if currently in comm loss state.
  uint32 commLost = 10 [(apex.width) = 1];
  // Padding for 8-byte alignment.
  repeated uint32 reserved = 11 [(apex.width) = 1, (apex.count) = 7];
}

message HilPlantTunableParams {
  // Vehicle mass [kg].
  double mass = 1 [(apex.default) = "10.0"];
  // Drag coefficient.
  double dragCd = 2 [(apex.default) = "0.5"];
  // Reference area [m^2].
  double dragArea = 3 [(apex.default) = "0.1"];
  // PD controller target altitude [m].
  double targetAlt = 4 [(apex.default) = "100.0"];
  // Proportional gain [N/m].
  double ctrlKp = 5 [(apex.default) = "2.0"];
  // Derivative gain [N/(m/s)].
  double ctrlKd = 6 [(apex.default) = "1.5"];
  // Maximum thrust magnitude [N].
  double thrustMax = 7 [(apex.default) = "200.0"];
  // Control frames without new cmd before comm loss (0=disabled).
  double commLossFrames = 8 [(apex.default) = "25.0"];
}
