// Generated by cdef_gen from the HilDriver spec -- DO NOT EDIT.
// Regenerate: make cdef (check-cdef diffs this file against the spec).
// Layout metadata rides apex/options.proto (standard FieldOptions
// extensions); declaration order is layout order.
syntax = "proto3";

package appsim.driver;

import "apex/options.proto";

message DriverState {
  // VehicleState frames sent to device.
  uint32 txCount = 1;
  // ControlCmd frames received from device.
  uint32 rxCount = 2;
  // CRC validation failures.
  uint32 crcErrors = 3;
  // sendState calls skipped (no UART or no state source).
  uint32 txMisses = 4;
  // recvCommand calls with no data available (non-blocking).
  uint32 rxMisses = 5;
  // Number of comm loss events.
  uint32 commLostCount = 6;
  // Detected gaps in received ControlCmd sequence.
  uint32 seqGaps = 7;
  // Last sent VehicleState.seqNum.
  uint32 txSeq = 8 [(apex.width) = 2];
  // Last received ControlCmd.seqNum.
  uint32 rxSeq = 9 [(apex.width) = 2];
  // 1 if a valid ControlCmd has been received.
  uint32 hasCmd = 10 [(apex.width) = 1];
  // 1 if UART is open and configured.
  uint32 uartOpen = 11 [(apex.width) = 1];
  // 1 if comm watchdog detected link loss.
  uint32 commLost = 12 [(apex.width) = 1];
  // Padding for alignment.
  uint32 reserved = 13 [(apex.width) = 1];
}
