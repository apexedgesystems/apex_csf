// Generated by cdef_gen from the SpecChannel spec -- DO NOT EDIT.
// Regenerate: make cdef (check-cdef diffs this file against the spec).
// Layout metadata rides apex/options.proto (standard FieldOptions
// extensions); declaration order is layout order.
syntax = "proto3";

package appsim.spec;

import "apex/options.proto";

message SpecChannelOutput {
  // gain * elapsed + offset.
  float value = 1;
  uint32 sequence = 2;
}

message SpecChannelState {
  // Seconds since boot (or last Zero).
  float elapsed = 1;
  // Zero commands handled.
  uint32 zeros = 2;
}

message SpecChannelTunableParams {
  // Ramp slope [units/s].
  float gain = 1 [(apex.default) = "1.0"];
  // Ramp intercept.
  float offset = 2 [(apex.default) = "0.0"];
  // Instance tag for operator displays.
  string channelTag = 3 [(apex.capacity) = 4];
}
