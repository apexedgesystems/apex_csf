// Generated by cdef_gen from the SpecMatrix spec -- DO NOT EDIT.
// Regenerate: make cdef (check-cdef diffs this file against the spec).
// Layout metadata rides apex/options.proto (standard FieldOptions
// extensions); declaration order is layout order.
syntax = "proto3";

package appsim.spec;

import "apex/options.proto";

message MatrixSnapshot {
  uint32 checksum = 1;
  uint32 stepCount = 2;
}

message SpecMatrixState {
  uint32 stepCount = 1;
  // Running XOR over the tunable bytes, recomputed each step.
  uint32 checksum = 2;
}

message SpecMatrixTunableParams {
  // int64 lane.
  int64 iWide = 1 [(apex.default) = "-8000000000"];
  // uint64 lane.
  uint64 uWide = 2 [(apex.default) = "9000000000"];
  // double lane.
  double precise = 3 [(apex.default) = "2.718281828"];
  // int32 lane.
  int32 iWord = 4 [(apex.default) = "-70000"];
  // uint32 lane.
  uint32 uWord = 5 [(apex.default) = "70000"];
  // float lane.
  float ratio = 6 [(apex.default) = "0.5"];
  // uint16 array lane.
  repeated uint32 gains = 7 [(apex.width) = 2, (apex.count) = 4];
  // int16 lane.
  int32 iShort = 8 [(apex.width) = 2, (apex.default) = "-300"];
  // uint16 lane.
  uint32 uShort = 9 [(apex.width) = 2, (apex.default) = "300"];
  // int8 lane.
  int32 iTiny = 10 [(apex.width) = 1, (apex.default) = "-5"];
  // uint8 lane.
  uint32 uTiny = 11 [(apex.width) = 1, (apex.default) = "5"];
  // bool lane.
  bool enabled = 12 [(apex.default) = "true"];
  // Bounded string lane (12-byte char buffer).
  string label = 13 [(apex.capacity) = 12];
  // String array lane (2 x 6-byte char buffers).
  repeated string tags = 14 [(apex.capacity) = 6, (apex.count) = 2];
  // Byte buffer lane.
  repeated uint32 blob = 15 [(apex.width) = 1, (apex.count) = 5];
}
