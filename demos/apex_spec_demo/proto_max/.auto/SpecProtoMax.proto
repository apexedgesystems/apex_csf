// Generated by cdef_gen from the SpecProtoMax spec -- DO NOT EDIT.
// Regenerate: make cdef (check-cdef diffs this file against the spec).
// Layout metadata rides apex/options.proto (standard FieldOptions
// extensions); declaration order is layout order.
syntax = "proto3";

package appsim.spec;

import "apex/options.proto";

message ReportResponse {
  // Current checksum.
  uint32 checksum = 1;
  // Steps taken.
  uint32 stepCount = 2;
}

message SpecProtoMaxState {
  // Steps taken.
  uint32 stepCount = 1;
  // XOR checksum over the tunable bytes, recomputed each step.
  uint32 checksum = 2;
}

message SpecProtoMaxTunableParams {
  // double lane.
  double precise = 1 [(apex.default) = "3.141592653589793"];
  // uint64 lane.
  uint64 uWide = 2 [(apex.default) = "9000000000"];
  // int64 lane.
  int64 iWide = 3 [(apex.default) = "-8000000000"];
  // float lane.
  float ratio = 4 [(apex.default) = "0.25"];
  // uint32 natural-width lane.
  uint32 uWord = 5 [(apex.default) = "70000"];
  // int32 natural-width lane.
  int32 iWord = 6 [(apex.default) = "-70000"];
  // Bounded float array lane.
  repeated float taps = 7 [(apex.count) = 3];
  // uint16 via width narrowing.
  uint32 uShort = 8 [(apex.width) = 2, (apex.default) = "300"];
  // int16 via width narrowing.
  int32 iShort = 9 [(apex.width) = 2, (apex.default) = "-300"];
  // Bounded string lane (8-byte char buffer, null-padded).
  string tag = 10 [(apex.capacity) = 8];
  // String array lane (2 x 4-byte char buffers).
  repeated string aliases = 11 [(apex.capacity) = 4, (apex.count) = 2];
  // Byte buffer lane (fixed 4 bytes).
  repeated uint32 cookie = 12 [(apex.width) = 1, (apex.count) = 4];
  // uint8 via width narrowing.
  uint32 uTiny = 13 [(apex.width) = 1, (apex.default) = "5"];
  // int8 via width narrowing.
  int32 iTiny = 14 [(apex.width) = 1, (apex.default) = "-5"];
  // bool lane.
  bool enabled = 15 [(apex.default) = "true"];
  // Tail padding to the struct's 8-byte alignment boundary.
  repeated uint32 reserved = 16 [(apex.width) = 1, (apex.count) = 5];
}
