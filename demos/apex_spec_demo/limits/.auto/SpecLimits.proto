// Generated by cdef_gen from the SpecLimits spec -- DO NOT EDIT.
// Regenerate: make cdef (check-cdef diffs this file against the spec).
// Layout metadata rides apex/options.proto (standard FieldOptions
// extensions); declaration order is layout order.
syntax = "proto3";

package appsim.spec;

import "apex/options.proto";

message NudgeRequest {
  // Added to value; results outside the banded rail reject.
  float delta = 1;
}

message SpecLimitsState {
  // Accumulated nudges, clamped to the banded rail.
  float value = 1;
  uint32 nudges = 2;
  uint32 rejects = 3;
}

message SpecLimitsTunableParams {
  // min-only rail.
  float floorOnly = 1 [(apex.default) = "1.0"];
  // max-only rail.
  float ceilOnly = 2 [(apex.default) = "50.0"];
  // min+max rail.
  float banded = 3 [(apex.default) = "5.0"];
  // step rail (granularity 0.25 from 0).
  float stepped = 4 [(apex.default) = "0.75"];
  // Array rail: every element inherits the same band.
  repeated float gainTable = 5 [(apex.count) = 4];
  // allowed-list rail (odd modes only).
  uint32 mode = 6 [(apex.width) = 1, (apex.default) = "3"];
  repeated uint32 reserved = 7 [(apex.width) = 1, (apex.count) = 3];
}
