# ==============================================================================
# ApexSpecDemo release manifest
#
# Pure SIL spec-born sensor demo proving the spec-driven codegen chain.
# RPi only (no firmware targets).
# ==============================================================================

APP_REGISTRY += ApexSpecDemo

APP_ApexSpecDemo_PLATFORMS          := rpi
APP_ApexSpecDemo_rpi_TYPE           := posix
APP_ApexSpecDemo_rpi_BINARY         := ApexSpecDemo
