# ==============================================================================
# demos/apex_spec_demo - the spec-driven demo
#
# Every data structure, the command dispatch, and the component
# skeleton originate from sensor/apex_data.toml. The demo exists to
# prove the spec-driven path end to end and to serve as the reference
# for building components from specs.
# ==============================================================================

add_subdirectory(sensor)
add_subdirectory(exec)
