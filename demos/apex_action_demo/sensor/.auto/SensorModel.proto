// Generated by cdef_gen from the SensorModel spec -- DO NOT EDIT.
// Regenerate: make cdef (check-cdef diffs this file against the spec).
// Layout metadata rides apex/options.proto (standard FieldOptions
// extensions); declaration order is layout order.
syntax = "proto3";

package appsim.sensor;

import "apex/options.proto";

message SensorHealthTlm {
  float temperature = 1;
  float rampRate = 2;
  uint32 stepCount = 3;
  uint32 wrapCount = 4;
  uint32 overtemp = 5 [(apex.width) = 1];
  repeated uint32 reserved = 6 [(apex.width) = 1, (apex.count) = 3];
}

message SensorOutput {
  // Current temperature reading [deg].
  float temperature = 1 [(apex.default) = "0.0"];
  // Rate of change [deg/step].
  float temperatureRate = 2 [(apex.default) = "0.0"];
  // 1 if temperature > 100.0 (threshold).
  uint32 overtemp = 3 [(apex.width) = 1];
  // Alignment padding.
  repeated uint32 reserved = 4 [(apex.width) = 1, (apex.count) = 3];
}

message SensorState {
  // Total step invocations.
  uint32 stepCount = 1;
  // Number of temperature wrap-arounds.
  uint32 wrapCount = 2;
}

message SensorTunableParams {
  // Temperature increase per step [deg/step].
  float rampRate = 1 [(apex.default) = "0.5"];
  // Starting temperature [deg].
  float initialTemp = 2 [(apex.default) = "20.0"];
  // Wrap-around temperature (resets to initial).
  float maxTemp = 3 [(apex.default) = "120.0"];
}
